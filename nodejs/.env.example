# 服务器端口
PORT=3000

# PostgreSQL 连接 URI（必须修改为你的实际数据库信息！）
# 格式：postgresql://用户名:密码@主机IP:端口/数据库名
DB_URI=postgres://postgres:yourpassword@localhost:5432/db

# JWT 配置（接口鉴权用）
JWT_SECRET=duochouchouai_jwt_2025
JWT_EXPIRES_IN=1h
